{% extends 'basesimple.html.twig' %}

{% block body %}
    <h1>Betum</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ betum.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ betum.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('Jus_Simple_beta_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('Jus_Simple_beta_edit', { 'id': betum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
