{% extends 'basesimple.html.twig' %}

{% block body %}
    <h1>Alpha</h1>

    <ol>
               <li>id: {{ alpha.id }} &nbsp; {{ alpha.title }}
          
                                {% if alpha.betas %} <ul> {% for beta in alpha.betas %} <li> {{ beta.title  }}  
				{% if beta.gammas %}  <ul>
                {% for gamma in beta.gammas %}  <li>{{ gamma.title}}   </li> {% endfor %} </ul> {% endif %} 
               			</li> {% endfor %} {% endif %} </li>
            </ol>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('Jus_Simple_alpha_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('Jus_Simple_alpha_edit', { 'id': alpha.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
